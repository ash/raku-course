---
title: Übung ’Eine Punktklasse‘
translations_gpt:
---

{% include menu.html %}

## Aufgabe

Definieren Sie eine Klasse `Point` mit zwei Attributen, `x` und `y`. Erzeugen Sie **zwei** Punkte — einen bei `(3, 4)` und einen im Ursprung `(0, 0)` — und geben Sie den Abstand zwischen den beiden Punkten aus.

## Beispiel

Das Programm gibt aus:

```
5
```

## Lösung

✅ [Siehe die Lösung](solution)

{% include nav.html %}
