---
title: Übung ’Ausgeben ohne Zeilenumbruch‘
translations_gpt:
---

{% include menu.html %}

## Aufgabe

Geben Sie über den Handle der Standardausgabe `$*OUT` die Buchstaben `a` und `b` mit `print` aus (das keinen Zeilenumbruch anfügt) und dann den Buchstaben `c` mit `say`. Die drei Buchstaben sollen in einer einzigen Zeile landen.

## Beispiel

Das Programm gibt aus:

```
abc
```

## Lösung

✅ [Siehe die Lösung](solution)

{% include nav.html %}
