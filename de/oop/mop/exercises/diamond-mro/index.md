---
title: Übung ’Eine Raute aus Klassen‘
translations_gpt:
---

{% include menu.html %}

## Aufgabe

Definieren Sie eine Klasse `A`. Definieren Sie dann `B` und `C`, die beide von `A` erben. Definieren Sie schließlich `D`, die sowohl von `B` als auch von `C` erbt. Geben Sie die method resolution order von `D` als Liste von Typnamen aus.

## Beispiel

Das Programm gibt aus:

```
(D B C A Any Mu)
```

## Lösung

✅ [Siehe die Lösung](solution)

{% include nav.html %}
