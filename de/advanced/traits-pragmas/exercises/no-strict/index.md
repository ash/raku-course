---
title: strict lockern
translations_gpt:
---

{% include menu.html %}

## Problem

Das `strict`-Pragma von Raku ist standardmäßig aktiviert, sodass jede Variable vor der Verwendung mit `my` deklariert werden muss. Deaktivieren Sie es mit `no strict` und weisen Sie dann, *ohne* ein `my`, `10` an `$a` und `20` an `$b` zu und geben Sie deren Summe aus.

## Beispiel

Das Programm gibt aus:

```
30
```

## Lösung

✅ [Siehe die Lösung](solution)

{% include nav.html %}
