---
title: Eine innere Summe
translations_gpt:
---

{% include menu.html %}

## Problem

Schreiben Sie eine Unterroutine `compute`, die einen verschachtelten Helfer `add` definiert, der die Summe seiner zwei Argumente zurückgibt. Geben Sie innerhalb von `compute` das Ergebnis von `add(2, 3) + add(4, 5)` zurück.

Geben Sie das Ergebnis des Aufrufs von `compute` aus.

## Beispiel

Das Programm gibt aus:

```
14
```

## Lösung

✅ [Siehe die Lösung](solution)

{% include nav.html %}
