---
title: Quiz — Verschachtelter Gültigkeitsbereich
translations_gpt:
---

{% include menu.html %}

Eine Unterroutine `inner` wird innerhalb einer anderen Unterroutine `outer` definiert. Von wo aus kann `inner` aufgerufen werden?

{:.quiz}
1 | nur von innerhalb von `outer`
0 | von überall im Programm
0 | nur von innerhalb einer anderen Unterroutine
0 | nur bevor `outer` definiert wird

{% include quiz.html %}

{% include nav.html %}
