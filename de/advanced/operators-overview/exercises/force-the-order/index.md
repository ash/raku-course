---
title: Reihenfolge erzwingen
translations_gpt:
---

{% include menu.html %}

## Aufgabe

Der Operator `**` bindet stärker als `+`, daher wird `2 ** 3 + 1` als `(2 ** 3) + 1` gelesen, was `9` ergibt. Fügen Sie Klammern hinzu, um diesen Vorrang zu überschreiben, sodass die Addition *zuerst* ausgeführt wird, und geben Sie das Ergebnis aus.

## Beispiel

Das Programm gibt aus:

```
16
```

## Lösung

✅ [Siehe die Lösung](solution)

{% include nav.html %}
