---
title: Übung ’Zweimal anwenden‘
translations_gpt:
---

{% include menu.html %}

## Aufgabe

Schreiben Sie eine Subroutine `twice`, die eine Subroutine `&f` und einen Wert `$x` aufnimmt und `&f` **zweimal** auf `$x` anwendet (also `f(f($x))`). Prüfen Sie sie, indem Sie `2` zweimal verdreifachen.

## Beispiel

Das Programm gibt aus:

```
18
```

## Lösung

✅ [Siehe die Lösung](solution)

{% include nav.html %}
