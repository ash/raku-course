---
title: Übung ’Großschreiben mit react‘
translations_gpt:
---

{% include menu.html %}

## Aufgabe

Sammeln Sie mit einem `react`-Block und einem einzigen `whenever` die Werte eines Supply, das aus den Zeichenketten `'a'`, `'b'`, `'c'` gebaut ist, und legen Sie jeden **großgeschrieben** in ein Array. Geben Sie das Array aus, nachdem der react-Block fertig ist.

## Beispiel

Das Programm gibt aus:

```
[A B C]
```

## Lösung

✅ [Siehe die Lösung](solution)

{% include nav.html %}
