---
title: Übung ’Regex gegen Token‘
translations_gpt:
---

{% include menu.html %}

## Aufgabe

Zeigen Sie, welchen Unterschied das Zurückweichen macht. Deklarieren Sie **dasselbe** Muster — eine oder mehrere Ziffern gefolgt von einer wörtlichen `5` — zweimal: einmal als `regex` und einmal als `token`. Prüfen Sie beide an `'12345'` und geben Sie für jedes ein schlichtes `True` oder `False` aus.

## Beispiel

Das Programm gibt aus:

```
True
False
```

## Lösung

✅ [Siehe die Lösung](solution)

{% include nav.html %}
