/* The Final Test — question bank (Parts 1–5), 100 items.
 * Fields: { part, topic, type, q, code?, options?, answer, explain }
 *   choice: answer = index · multi: answer = [indices] · boolean: answer = true|false
 *   input: answer = string OR [accepted strings] (case-insensitive, whitespace-collapsed)
 * q/explain may use `backticks` for inline code. Generated — edit with care.
 */
window.FINAL_TEST_QUESTIONS = [
{ part: 1, topic: 'Raku vs. Rakudo', type: 'boolean',
    q: 'In this course, `Raku` is the name of the programming language and `Rakudo` is the name of the compiler.',
    answer: true,
    explain: 'Raku is the language; Rakudo is the compiler (running on MoarVM).' },

  { part: 1, topic: 'Hello, World', type: 'multi',
    q: 'Which of these lines correctly print `Hello, World!` (with or without a trailing newline)?',
    options: ['`say \'Hello, World!\';`','`put \'Hello, World!\';`','`\'Hello, World!\'.say;`','`echo \'Hello, World!\';`'],
    answer: [0,1,2],
    explain: 'Raku uses `say`/`put`/`print`, and routines like `say` can be called as methods; there is no `echo` routine.' },

  { part: 1, topic: 'Input', type: 'input',
    q: 'Which built-in routine reads a line of input from the user and returns it as a string? Type its name.',
    answer: 'prompt',
    explain: 'The `prompt` routine waits for input and returns it as a string.' },

  { part: 1, topic: 'Comments', type: 'boolean',
    q: 'A single-line comment in Raku starts with `//` and continues to the end of the line.',
    answer: false,
    explain: 'A single-line comment starts with `#`, not `//` — in Raku `//` is the defined-or operator, not a comment.' },

  { part: 1, topic: 'Comments', type: 'input',
    q: 'Which two-character combination begins a multi-line (or embedded) comment, before its enclosing brackets? Type it exactly.',
    answer: ['#`'],
    explain: 'Multi-line and embedded comments start with a `#` followed by a backtick and then a bracket pair such as `( )` or `{ }`.' },

  { part: 1, topic: 'Scalar variables', type: 'input',
    q: 'Which declarator introduces a new lexical variable, as in `___ $name;`? Type the keyword.',
    answer: 'my',
    explain: 'Variables are declared with the `my` declarator.' },

  { part: 1, topic: 'Scalar variables', type: 'boolean',
    q: 'In Raku, variable names (identifiers) are case insensitive, so `$name` and `$Name` refer to the same variable.',
    answer: false,
    explain: 'Identifiers are case sensitive: `$name`, `$Name`, and `$NAME` are three different variables.' },

  { part: 1, topic: 'Defined-or operator', type: 'choice',
    code: 'my $b;\nsay $b // \'delta\';',
    q: 'What does this program print?',
    options: ['`delta`','`Nil`','`(Any)`','an error'],
    answer: 0,
    explain: 'The defined-or operator `//` returns the right operand when the left one is undefined.' },

  { part: 1, topic: 'String concatenation', type: 'input',
    q: 'Which operator concatenates two strings in Raku? Type the single character.',
    answer: ['~'],
    explain: 'The tilde `~` is the string concatenation operator.' },

  { part: 1, topic: 'String length', type: 'choice',
    code: "say '你好世界'.chars;",
    q: 'What does this print?',
    options: ['`4`','`12`','`8`','`1`'],
    answer: 0,
    explain: 'The `chars` method counts characters; this Chinese greeting has 4 characters (its byte count is 12).' },

  { part: 1, topic: 'String interpolation', type: 'boolean',
    q: 'Variables are interpolated inside single-quoted strings but NOT inside double-quoted strings.',
    answer: false,
    explain: 'It is the other way round: double quotes interpolate variables and code, while single quotes show the characters as-is.' },

  { part: 1, topic: 'Escaping / interpolation', type: 'choice',
    code: "say 'One\\nTwo';\nsay \"Three\\nFour\";",
    q: 'What is printed?',
    options: [
      '`One\\nTwo` on one line, then `Three` and `Four` on two lines',
      '`One` and `Two` on two lines, then `Three` and `Four` on two lines',
      '`One\\nTwo` and `Three\\nFour`, each on one line',
      '`One Two` then `Three Four`'],
    answer: 0,
    explain: 'In single quotes `\\n` is literal; in double quotes `\\n` becomes a newline.' },

  { part: 1, topic: 'Integer numbers', type: 'multi',
    q: 'Select all valid ways to write integer literals in Raku.',
    options: ['`1_000_000`','`-42`','`50,000`','`100_`'],
    answer: [0,1],
    explain: 'Underscores may group digits, and a leading `-` is fine; commas are not separators and a number cannot end with `_`.' },

  { part: 1, topic: 'Rational numbers', type: 'choice',
    code: 'say 0.1 + 0.2 - 0.3;',
    q: 'What does Raku print?',
    options: ['`0`','`5.5511151231257827e-17`','`0.0000000001`','`0.6`'],
    answer: 0,
    explain: 'Decimals like `0.1` are `Rat` (fractions), so the arithmetic is exact and the result is `0`.' },

  { part: 1, topic: 'Number types', type: 'choice',
    code: 'say (0.5).WHAT;',
    q: 'What type does the literal `0.5` have?',
    options: ['`(Rat)`','`(Num)`','`(Int)`','`(Str)`'],
    answer: 0,
    explain: 'A decimal literal such as `0.5` is a rational number, of type `Rat`.' },

  { part: 1, topic: 'Number operations', type: 'input',
    q: 'Which operator tests divisibility, returning `True` if the left operand is divisible by the right (e.g. `10 __ 5`)? Type the operator.',
    answer: ['%%'],
    explain: 'The `%%` operator is the divisibility test; `10 %% 5` is `True`.' },

  { part: 1, topic: 'Number operations', type: 'choice',
    code: 'say -10 % 3;',
    q: 'What is the result of `-10 % 3` in Raku?',
    options: ['`2`','`-1`','`1`','`-2`'],
    answer: 0,
    explain: '`$a % $b` equals `$a - $b * floor($a / $b)`, so `-10 % 3` is `2`.' },

  { part: 1, topic: 'Boolean type', type: 'multi',
    q: 'Which of these are correct Boolean literal values in Raku?',
    options: ['`True`','`False`','`TRUE`','`1`'],
    answer: [0,1],
    explain: '`True` and `False` are the only Boolean literals; `TRUE` is not one and `1` is an integer.' },

  { part: 1, topic: 'Boolean operations', type: 'choice',
    code: "say 'Hello' && 'World';",
    q: 'What does this print?',
    options: ['`World`','`True`','`Hello`','`Bool::True`'],
    answer: 0,
    explain: '`&&` returns the last argument when none is false; with two truthy strings it returns `World`.' },

  { part: 1, topic: 'Ranges', type: 'choice',
    code: 'say (1 ..^ 5).elems;',
    q: 'How many elements does the range `1 ..^ 5` generate?',
    options: ['`4`','`5`','`3`','`6`'],
    answer: 0,
    explain: 'The `^` excludes the upper endpoint, so the range is `1, 2, 3, 4` — four elements.' },

  { part: 1, topic: 'Smart-match', type: 'input',
    q: 'Which operator checks whether a value matches (falls within) a range, as in `$v ___ $r`? Type it.',
    answer: ['~~'],
    explain: 'The smart-match operator `~~` tests a value against a range (and many other things).' },

  { part: 1, topic: 'Code blocks / scope', type: 'choice',
    code: 'my $x = 1;\n{\n    my $x = 42;\n    say $x;\n}\nsay $x;',
    q: 'What does this program print?',
    options: ['`42` then `1`','`1` then `42`','`42` then `42`','`1` then `1`'],
    answer: 0,
    explain: 'The inner `my $x` is a separate local variable; inside the block it is `42`, outside it stays `1`.' },

  { part: 1, topic: 'Conditional checks', type: 'multi',
    q: 'Which statements about conditionals in Raku are correct?',
    options: [
      'The chained-elsif keyword is spelled `elsif`',
      '`unless` cannot be followed by an `else` block',
      'Parentheses around an `if` condition are required',
      'The ternary operator is written `?? ... !!`'],
    answer: [0,1,3],
    explain: 'It is `elsif`, `unless` takes no `else`, and the ternary is `?? !!`; parentheses around `if` conditions are optional.' },

  { part: 1, topic: 'Comparing strings', type: 'choice',
    q: 'Which operator tests two strings for equality?',
    options: ['`eq`','`==`','`=~=`','`is`'],
    answer: 0,
    explain: 'String comparison uses word operators; `eq` tests equality (`==` is for numbers).' },

  { part: 1, topic: 'Loops', type: 'choice',
    code: 'my $x = 10;\n$x -= 3 while $x > 2;\nsay $x;',
    q: 'What does this print?',
    options: ['`1`','`2`','`4`','`-2`'],
    answer: 0,
    explain: 'The `while` statement modifier subtracts 3 repeatedly (10→7→4→1) until the condition fails, leaving `1`.' },

  { part: 1, topic: 'Coercion', type: 'choice',
    code: 'say 42.WHAT;',
    q: 'What does `42.WHAT` print?',
    options: ['`(Int)`','`(Num)`','`(Rat)`','`Int`'],
    answer: 0,
    explain: 'The `WHAT` method reports the type in parentheses; `42` is `(Int)`.' },

  { part: 1, topic: 'Coercion prefixes', type: 'multi',
    q: 'Match the prefix coercion operators. Which pairings are correct?',
    options: [
      '`~` coerces to a string',
      '`+` coerces to a number',
      '`?` coerces to a Boolean',
      '`!` coerces to a string'],
    answer: [0,1,2],
    explain: '`~` is string, `+` is numeric, `?` is Boolean coercion; `!` is Boolean negation, not string coercion.' },

  { part: 1, topic: 'Typed variables', type: 'boolean',
    q: 'Given `my Int $var = 42;`, the assignment `$var = \'314E-2\';` raises a type-check error.',
    answer: true,
    explain: 'A typed `Int` container rejects a `Str`, producing "Type check failed... expected Int but got Str".' },

  { part: 1, topic: 'Arrays / hashes', type: 'multi',
    q: 'Which statements about arrays and hashes are correct?',
    options: [
      'Array variables use the `@` sigil',
      'Hash variables use the `%` sigil',
      'The `elems` method returns an array\'s length',
      'Array indexing starts at 1'],
    answer: [0,1,2],
    explain: 'Arrays use `@`, hashes use `%`, `elems` gives the size, and indices start at `0`.' },

  { part: 1, topic: 'Functions', type: 'choice',
    code: 'sub add($x, $y) { $x + $y }\nsay add(10, 20);',
    q: 'What does this print?',
    options: ['`30`','nothing (no `return`)','`Nil`','an error'],
    answer: 0,
    explain: 'A function returns its last evaluated value, so no explicit `return` is needed; this prints `30`.' },

{ part: 2, topic: 'Representations', type: 'choice',
    q: 'Which method does `say` use to turn a value into text?',
    options: ['`.gist`','`.Str`','`.raku`','`.perl`'],
    answer: 0,
    explain: '`say` prints the human-friendly `.gist` form, while `print`/`put` use `.Str` and `dd`/REPL use `.raku`.' },

  { part: 2, topic: 'Representations', type: 'input',
    q: 'What does this program print?',
    code: 'my @a = 1, 2, 3;\nsay @a.Str;',
    answer: ['1 2 3'],
    explain: '`.Str` joins the elements with spaces and drops the brackets, unlike `.gist` which shows `[1 2 3]`.' },

  { part: 2, topic: 'dd (data dumper)', type: 'boolean',
    q: 'The `dd` routine writes its output to standard output (STDOUT), the same stream as `say`.',
    answer: false,
    explain: '`dd` is a debugging tool that dumps the `.raku` form of its arguments to standard error (STDERR), not STDOUT.' },

  { part: 2, topic: 'Scalar containers', type: 'choice',
    q: 'Given `my Int $int;`, which assignment causes an error because Raku does no automatic casting?',
    options: ['`$int = 123;`','`$int = 100 + 23;`','`$int = \'123\'.Int;`','`$int = 123.45;`'],
    answer: 3,
    explain: 'Assigning the Rat `123.45` to an `Int` container fails; you must convert explicitly with `.Int`.' },

  { part: 2, topic: 'Ordered containers', type: 'input',
    q: 'What does this program print?',
    code: 'my @a = 1, 2, 3;\nmy @b = 4, 5;\nmy @c = @a, @b;\nsay @c.elems;',
    answer: ['2'],
    explain: 'Arrays do not auto-flatten when nested, so `@c` holds two array elements.' },

  { part: 2, topic: 'Binding', type: 'input',
    q: 'What does this program print?',
    code: 'my $x = 10;\nmy $y := $x;\n$x = 20;\nsay $y;',
    answer: ['20'],
    explain: 'Binding with `:=` makes `$y` refer to the same container as `$x`, so changing `$x` changes `$y`.' },

  { part: 2, topic: 'Contexts', type: 'choice',
    q: 'What does `say +@a` print for `my @a = 10, 20, 30;`?',
    options: ['`60`','`3`','`10 20 30`','`True`'],
    answer: 1,
    explain: 'Prefix `+` forces numeric context, and an array in numeric context becomes its element count (3).' },

  { part: 2, topic: 'Contexts', type: 'input',
    q: 'What does this program print?',
    code: 'my @a = 1, 2, 3;\nmy $n = 0;\n$n++ for $(@a);\nsay $n;',
    answer: ['1'],
    explain: '`$( )` forces item context, so the array is one single item and the loop runs only once.' },

  { part: 2, topic: 'Subscript adverbs', type: 'choice',
    q: 'What does this program print?',
    code: 'my %h = a => 1, b => 2;\nmy $v = %h<a>:delete;\nsay $v;\nsay %h.keys;',
    options: ['`1` then `(b)`','`(a 1)` then `(b)`','`True` then `(a b)`','`1` then `(a b)`'],
    answer: 0,
    explain: 'The `:delete` adverb removes the key from the hash and returns the value it held, so `$v` is `1` and only `b` remains.' },

  { part: 2, topic: 'Subscript adverbs', type: 'boolean',
    q: 'For `my %h = a => 1, b => 2;`, the expression `%h<z>:exists` returns `False`.',
    answer: true,
    explain: '`:exists` asks whether the key is present, and there is no `z` key in the hash.' },

  { part: 2, topic: 'Special variables', type: 'input',
    q: 'Which twigil marks a dynamic variable that is looked up through the call stack (e.g. `$*OUT`)? Give just the twigil character.',
    answer: ['*'],
    explain: 'The `*` twigil denotes a dynamic variable resolved through dynamic scope, unlike lexical variables.' },

  { part: 2, topic: 'Special variables', type: 'input',
    q: 'What does this program print?',
    code: 'my $*n = 1;\nsub show { say $*n }\nsub run {\n    my $*n = 2;\n    show();\n}\nrun();',
    answer: ['2'],
    explain: 'A dynamic variable is found through the call stack, so `show` sees the `$*n` set by its caller `run`.' },

  { part: 2, topic: 'Introspection', type: 'choice',
    q: 'What does `say $x.WHAT` print for an untyped, unassigned `my $x;`?',
    options: ['`(Any)`','`Any`','`(Nil)`','`(Scalar)`'],
    answer: 0,
    explain: 'An untyped variable starts as the `Any` type, and `.WHAT` prints the type object with parentheses.' },

  { part: 2, topic: 'Introspection', type: 'boolean',
    q: '`.WHAT` returns a type object printed with parentheses like `(Int)`, while `.^name` returns a bare string like `Int`.',
    answer: true,
    explain: '`.WHAT` yields the type object (shown as `(Int)`); `.^name` returns the name as a plain string.' },

  { part: 2, topic: 'Number bases', type: 'input',
    q: 'What does `say :3<12>;` print?',
    answer: ['5'],
    explain: 'The radix form `:3<12>` reads 12 in base 3, giving 1*3 + 2 = 5.' },

  { part: 2, topic: 'Native types', type: 'input',
    q: 'What does this program print?',
    code: 'my int8 $i = -128;\n$i--;\nsay $i;',
    answer: ['127'],
    explain: 'A sized native `int8` wraps around on overflow, so decrementing -128 rolls to 127.' },

  { part: 2, topic: 'Sets and bags', type: 'choice',
    q: 'What does `say (set(1, 2) ∪ set(2, 3)).elems;` print?',
    options: ['`4`','`3`','`2`','`1`'],
    answer: 1,
    explain: 'The union is the distinct set {1, 2, 3}, which has 3 elements.' },

  { part: 2, topic: 'Sequences', type: 'input',
    q: 'What does `say 1, 3, 9 ... 81;` print?',
    answer: ['(1 3 9 27 81)','1 3 9 27 81'],
    explain: 'Three seed values with a constant ratio make a geometric sequence multiplying by 3 up to 81.' },

  { part: 2, topic: 'Enumerations', type: 'choice',
    q: 'What does this program print?',
    code: 'enum Colour <red green blue>;\nsay green + blue;',
    options: ['`3`','`greenblue`','`1`','an error'],
    answer: 0,
    explain: 'Enum constants number from zero (red=0, green=1, blue=2), so green + blue is 1 + 2 = 3.' },

  { part: 2, topic: 'Metaoperators', type: 'multi',
    q: 'Which of these produce `(11 22 33)` from `(1, 2, 3)` and `(10, 20, 30)`? (select all that apply)',
    options: ['`(1,2,3) Z+ (10,20,30)`','`(1,2,3) »+» (10,20,30)`','`(1,2,3) X+ (10,20,30)`','`[+] (1,2,3),(10,20,30)`'],
    answer: [0,1],
    explain: 'Zip `Z+` and hyper `»+»` both add element-by-element; cross `X+` pairs every combination instead.' },

  { part: 2, topic: 'User-defined operators', type: 'choice',
    q: 'What does this program print?',
    code: 'sub infix:<times>($a, $b) { $a * $b }\nsay 6 times 7;',
    options: ['`42`','`67`','`times`','an error'],
    answer: 0,
    explain: 'Defining `infix:<times>` creates a new operator between two operands whose body multiplies them.' },

  { part: 2, topic: 'Phasers', type: 'boolean',
    q: 'A `BEGIN` phaser runs at run time, only after the whole program has finished compiling.',
    answer: false,
    explain: '`BEGIN` fires at compile time, as early as possible — not at run time.' },

  { part: 2, topic: 'gather/take', type: 'choice',
    q: 'What does this program print?',
    code: 'my @r = gather for 1..6 {\n    take $_ if $_ %% 3;\n}\nsay @r;',
    options: ['`[3 6]`','`[1 2 3 4 5 6]`','`[3]`','`[]`'],
    answer: 0,
    explain: '`gather` collects only the values passed to `take`, and the guard is true only for 3 and 6.' },

  { part: 2, topic: 'Signatures', type: 'boolean',
    q: 'In `sub f($first, *@rest) {...}`, calling `f(\'a\', \'b\', \'c\')` leaves `@rest` with 2 elements.',
    answer: true,
    explain: 'Fixed parameters fill first, so `$first` takes `a` and the slurpy `*@rest` collects the remaining two.' },

  { part: 2, topic: 'Whatever star', type: 'choice',
    q: 'What does this program print?',
    code: 'my @a = 5, 6, 7, 8, 9;\nsay @a[*-2];',
    options: ['`8`','`7`','`9`','`6`'],
    answer: 0,
    explain: 'In a subscript, bare `*` is the element count (5), so `*-2` is index 3, whose value is 8.' },

{ part: 3, topic: 'Classes', type: 'input',
  q: 'Which method do you call on a class name to create a new instance (object) of that class?',
  answer: ['new', '.new', 'new()'],
  explain: 'An instance is created by calling the `new` method on the class name, e.g. `Dog.new`.' },

{ part: 3, topic: 'Type objects', type: 'choice',
  q: 'What does this program print?',
  code: 'class Dog {}\nsay Dog.defined;\nsay Dog.new.defined;',
  options: ['`False` then `True`','`True` then `False`','`True` then `True`','`False` then `False`'],
  answer: 0,
  explain: 'A type object is undefined (`False`), while an instance made with `new` is defined (`True`).' },

{ part: 3, topic: 'Attributes', type: 'boolean',
  q: 'Declaring an attribute with `has $.name` automatically creates a public accessor method called `name`.',
  answer: true,
  explain: 'The `$.` form both declares the attribute and generates a same-named accessor method.' },

{ part: 3, topic: 'Read-write attributes', type: 'choice',
  q: 'By default, the accessor generated by `has $.name` is read-only. Which trait makes it writable from outside?',
  options: ['`is rw`','`is writable`','`is mutable`','`is public`'],
  answer: 0,
  explain: 'Marking the attribute `is rw` makes its accessor return a writable container so assignment works.' },

{ part: 3, topic: 'Default values', type: 'choice',
  q: 'What does this program print?',
  code: 'class Dog {\n    has Str $.name;\n    has Rat $.weight = 4.0;\n}\nsay Dog.new(name => \'Rex\').weight;',
  options: ['`4`','`0`','an error','`Nil`'],
  answer: 0,
  explain: 'With no `weight` passed to `new`, the attribute takes its default of `4.0`, shown as `4`.' },

{ part: 3, topic: 'Methods', type: 'input',
  q: 'Inside a method, which keyword refers to the object the method was called on?',
  answer: ['self'],
  explain: 'The invocant object is available inside a method as `self`.' },

{ part: 3, topic: 'Private attributes', type: 'boolean',
  q: 'An attribute declared with `has $!code` has no public accessor and cannot be read from outside the object.',
  answer: true,
  explain: 'The `$!` form declares a private attribute, reachable only inside the class’s own methods.' },

{ part: 3, topic: 'Class methods', type: 'boolean',
  q: 'The `new` method that creates objects is an instance method — you must already have an object before you can call it.',
  answer: false,
  explain: '`new` is called on the class (the type object), not on an existing instance, so it is a class method.' },

{ part: 3, topic: 'Subs vs methods', type: 'choice',
  q: 'What is the key difference between a subroutine and a method?',
  options: ['A method belongs to a class and is called on an object with the dot; a subroutine stands alone and takes all data as arguments','A subroutine can return a value but a method cannot','A method cannot take parameters, a subroutine can','There is no difference; the keywords are interchangeable'],
  answer: 0,
  explain: 'A method is called on an object and uses its state via `self`; a sub stands alone and receives everything as arguments.' },

{ part: 3, topic: 'Inheritance', type: 'input',
  q: 'Which trait, written after a class name, makes it inherit from a parent class (e.g. `class Dog ___ Animal`)?',
  answer: ['is'],
  explain: '`class Dog is Animal` makes `Dog` inherit the attributes and methods of `Animal`.' },

{ part: 3, topic: 'Overriding', type: 'choice',
  q: 'What does this program print?',
  code: 'class Animal {\n    has $.name;\n    method speak { \'some sound\' }\n    method intro { "$.name says " ~ self.speak }\n}\nclass Dog is Animal {\n    method speak { \'Woof\' }\n}\nsay Dog.new(name => \'Rex\').intro;',
  options: ['`Rex says Woof`','`Rex says some sound`','`some sound`','an error'],
  answer: 0,
  explain: 'The inherited `intro` calls `self.speak`, which resolves to the Dog’s overriding `speak`.' },

{ part: 3, topic: 'Roles', type: 'multi',
  q: 'Which of the following are true about roles? (Select all that apply.)',
  options: ['A role is given to a class with the `does` trait','A class can compose more than one role','A role describes something a class *can do*, rather than what it *is*','A role can only be used through inheritance with `is`'],
  answer: [0,1,2],
  explain: 'Roles are composed with `does` (one or several) and express capability; they are not applied via `is`.' },

{ part: 3, topic: 'Mixins', type: 'choice',
  q: 'What does this program print?',
  code: 'role Loud { method greet { callsame().uc } }\nclass Greeter { method greet { \'Hello\' } }\nmy $loud = Greeter.new but Loud;\nsay $loud.greet;',
  options: ['`HELLO`','`Hello`','`hello`','an error'],
  answer: 0,
  explain: 'The `but Loud` mixin overrides `greet`; `callsame` runs the original and `.uc` upper-cases it.' },

{ part: 3, topic: 'MOP', type: 'input',
  q: 'Which meta-method (written with the `.^` syntax) returns a type’s method resolution order / inheritance chain?',
  answer: ['mro', '^mro', '.^mro'],
  explain: '`.^mro` returns the method resolution order — the chain of types Raku searches.' },

{ part: 3, topic: 'Standard streams', type: 'choice',
  q: 'Which routine writes its text to standard error rather than standard output?',
  options: ['`note`','`say`','`print`','`put`'],
  answer: 0,
  explain: '`note` writes to `$*ERR` (standard error); `say`, `print`, and `put` go to `$*OUT`.' },

{ part: 3, topic: 'Files', type: 'multi',
  q: 'Which statements about `spurt` and `slurp` are correct? (Select all that apply.)',
  options: ['`spurt` writes a string to a file, creating or replacing it','`slurp` reads a whole file’s contents into a string','Passing `:append` to `spurt` adds to the end instead of replacing','`slurp` deletes the file after reading it'],
  answer: [0,1,2],
  explain: '`spurt` writes (with `:append` to add), `slurp` reads the whole file; neither deletes anything.' },

{ part: 3, topic: 'Deleting', type: 'boolean',
  q: '`rmdir` will remove a directory even when it still contains files.',
  answer: false,
  explain: '`rmdir` removes only an empty directory; a non-empty one is left untouched.' },

{ part: 3, topic: 'External programs', type: 'choice',
  q: 'What does this program print?',
  code: 'my $proc = run \'sh\', \'-c\', \'exit 3\';\nsay $proc.exitcode;',
  options: ['`3`','`0`','`True`','an error'],
  answer: 0,
  explain: 'A `Proc`’s `.exitcode` reports the program’s exit code, here `3`.' },

{ part: 3, topic: 'try', type: 'choice',
  q: 'After a `try` block catches an exception, what happens to the value the block evaluates to and to `$!`?',
  options: ['The block evaluates to an undefined value, and the caught exception is stored in `$!`','The block re-throws and the program stops','The block returns the exception message as a string, and `$!` is empty','The whole program restarts'],
  answer: 0,
  explain: 'A failed `try` block yields an undefined value while the caught exception is available in `$!`.' },

{ part: 3, topic: 'Custom exceptions', type: 'choice',
  q: 'What must a custom exception class do to be usable as an exception?',
  code: 'class TooBig is ______ {\n    has $.value;\n    method message { "Value $.value is too big" }\n}',
  options: ['Inherit from `Exception` and provide a `message` method','Inherit from `X::AdHoc`','Inherit from `Failure` and call `fail`','Nothing special; any class can be thrown'],
  answer: 0,
  explain: 'A custom exception inherits from the built-in `Exception` class and supplies a `message` method, then is thrown with `.throw`.' },

{ part: 4, topic: 'Literal matching', type: 'boolean',
  q: 'Inside a Raku regex, an unquoted space in the pattern is significant and must match a real space in the string.',
  answer: false,
  explain: 'By default spaces in a regex are insignificant and only aid readability; to match a real space you quote it or use `\\s` (or turn on `:s`).' },

{ part: 4, topic: 'Character classes', type: 'input',
  q: 'To match any single character that is NOT in a set, which single symbol do you put right after the opening bracket, as in `<-[a..z]>`?',
  answer: ['-', 'minus', 'hyphen', 'dash'],
  explain: 'A minus sign after `<[` negates the character class, matching any character not listed.' },

{ part: 4, topic: 'Predefined classes', type: 'choice',
  q: 'Which predefined character class matches a single digit?',
  options: ['`\\d`','`\\w`','`\\s`','`\\D`'],
  answer: 0,
  explain: '`\\d` matches a digit; `\\w` is a word character, `\\s` is whitespace, and `\\D` is a non-digit.' },

{ part: 4, topic: 'Quantifiers', type: 'choice',
  q: 'Which quantifier makes the item before it optional, matching it zero or one time?',
  options: ['`*`','`+`','`?`','`**`'],
  answer: 2,
  explain: '`?` means zero or one occurrence; `*` is zero or more and `+` is one or more.' },

{ part: 4, topic: 'Greedy vs frugal', type: 'choice',
  q: 'What does this match?',
  code: "say ('<a><b>' ~~ / '<' .+? '>' /).Str;",
  options: ['`<a>`','`<a><b>`','`<b>`','`a`'],
  answer: 0,
  explain: 'The `?` after `+` makes it frugal, so `.+?` matches as little as possible and stops at the first `>`.' },

{ part: 4, topic: 'Match object', type: 'input',
  q: 'After a successful match, which special variable holds the resulting Match object?',
  answer: ['$/'],
  explain: 'The match is stored in `$/`; individual captures are then available as `$0`, `$1`, and so on.' },

{ part: 4, topic: 'Named captures', type: 'choice',
  q: 'How do you write a named capture called `age` around the sub-pattern `(\\d+)`?',
  options: ['`$<age>=(\\d+)`','`(?<age>\\d+)`','`<age: \\d+>`','`$age=(\\d+)`'],
  answer: 0,
  explain: 'Raku names a capture with `$<name>=( … )`, reachable afterwards as `$<name>`.' },

{ part: 4, topic: 'Alternation', type: 'choice',
  q: 'What does this match?',
  code: "say ('catdog' ~~ / cat | catdog /).Str;",
  options: ['`cat`','`catdog`','`dog`','no match'],
  answer: 1,
  explain: 'The single bar `|` uses longest-token matching, so it prefers the longer alternative `catdog` regardless of order.' },

{ part: 4, topic: 'Anchors', type: 'boolean',
  q: 'Wrapping a pattern in `^` and `$`, as in `/^ \\d+ $/`, requires the pattern to match the entire string with nothing left over.',
  answer: true,
  explain: '`^` anchors at the start and `$` at the end, so together they force a whole-string match.' },

{ part: 4, topic: 'Substitution', type: 'input',
  q: 'Which adverb added to `s///` (or `m///`) makes it replace or match every occurrence rather than only the first?',
  answer: [':g', 'g', ':global', 'global'],
  explain: 'The `:g` (global) adverb applies the operation to every match instead of just the first.' },

{ part: 4, topic: 'token vs regex', type: 'boolean',
  q: 'A `token` backtracks the same way a `regex` does when a later part of the pattern fails.',
  answer: false,
  explain: 'A `regex` backtracks, but a `token` does not — it commits to its match, which is why `token` is preferred in grammars.' },

{ part: 4, topic: 'rule vs token', type: 'multi',
  q: 'Which statements about `rule` in a grammar are true?',
  options: ['`rule` treats whitespace in the pattern as significant','`rule` behaves as if the `:s` adverb is on','`rule` backtracks like a `regex`','`rule` is typically used for higher-level structure separated by spaces'],
  answer: [0,1,3],
  explain: 'A `rule` is a `token` (non-backtracking) with significant whitespace, equivalent to `:s`, and suits space-separated structure.' },

{ part: 4, topic: 'make and made', type: 'input',
  q: 'Inside a token you call `make` to attach a value to the match. Which method (or its `.ast` alias) reads that value back afterwards?',
  answer: ['made', '.made'],
  explain: '`made` (alias `.ast`) retrieves the value stored on a match by `make`.' },

{ part: 5, topic: 'Recursion', type: 'input',
    q: 'What does this program print?',
    code: 'sub fact($n) {\n    $n <= 1 ?? 1 !! $n * fact($n - 1);\n}\nsay fact(5);',
    answer: ['120'],
    explain: 'The subroutine recurses down to the base case `$n <= 1`, computing 5 × 4 × 3 × 2 × 1 = 120.' },

  { part: 5, topic: 'Higher-order functions', type: 'choice',
    q: 'What does this program print?',
    code: 'sub adder($n) {\n    sub ($x) { $x + $n };\n}\n\nmy &add5 = adder(5);\nsay add5(3);',
    options: ['`5`', '`3`', '`8`', '`&add5`'],
    answer: 2,
    explain: '`adder(5)` returns a sub that adds 5, so `add5(3)` is 8 — the returned closure remembers `$n`.' },

  { part: 5, topic: 'Closures', type: 'boolean',
    q: 'In Raku, a subroutine returned from another subroutine can keep using a variable from the enclosing scope even after that outer subroutine has finished.',
    answer: true,
    explain: 'That captured, persistent state is exactly what makes it a closure.' },

  { part: 5, topic: 'Reduction', type: 'input',
    q: 'Complete the reduction that concatenates a list of strings into one: `say [__] "a", "b", "c";` prints `abc`. Which operator goes in the brackets?',
    answer: ['~'],
    explain: '`[~]` reduces with the string-concatenation operator `~`, gluing the pieces into `abc`.' },

  { part: 5, topic: 'Feeds', type: 'choice',
    q: 'What does this program print?',
    code: '(1..10)\n    ==> grep(* %% 2)\n    ==> map(* ** 2)\n    ==> my @result;\n\nsay @result;',
    options: ['`[2 4 6 8 10]`', '`[4 16 36 64 100]`', '`[1 4 9 16 25]`', '`[1 3 5 7 9]`'],
    answer: 1,
    explain: 'The feed keeps the even numbers 2,4,6,8,10 then squares them to 4,16,36,64,100.' },

  { part: 5, topic: 'Lazy sequences', type: 'choice',
    q: 'What does `say (1, 1, * + * ... *)[^8];` print?',
    code: 'say (1, 1, * + * ... *)[^8];',
    options: ['`(1 1 2 3 5 8 13 21)`', '`(1 2 3 4 5 6 7 8)`', '`(1 1 2 4 8 16 32 64)`', '`(2 4 6 8 10 12 14 16)`'],
    answer: 0,
    explain: 'The closure `* + *` sums the two previous elements, giving the Fibonacci sequence; only the first eight are computed.' },

  { part: 5, topic: 'Junctions', type: 'choice',
    q: 'What does this program print?',
    code: 'my $j = 1 | 2 | 3;\nsay $j + 10;',
    options: ['`13`', '`any(11, 12, 13)`', '`36`', '`all(11, 12, 13)`'],
    answer: 1,
    explain: 'Autothreading applies `+ 10` to each value of the any-junction, giving `any(11, 12, 13)`.' },

  { part: 5, topic: 'Threads', type: 'input',
    q: 'You start a thread with `my $t = Thread.start({ ... });`. Which method do you call on `$t` to make the main program wait until that thread has completed? (give the method name, without dot or parentheses)',
    answer: ['finish', 'join'],
    explain: '`.finish` (also spelled `.join`) blocks until the thread is done.' },

  { part: 5, topic: 'Promises', type: 'choice',
    q: 'What does this program print?',
    code: 'my $p = start { 10 };\nmy $q = $p.then({ .result * 2 });\nsay await $q;',
    options: ['`10`', '`20`', '`12`', '`(10 20)`'],
    answer: 1,
    explain: '`.then` runs after `$p` completes; inside it `.result` is 10, doubled to 20.' },

  { part: 5, topic: 'Channels', type: 'boolean',
    q: 'Calling `.list` on a channel returns immediately with whatever values are queued, without waiting for the channel to be closed.',
    answer: false,
    explain: '`.list` blocks until the channel is closed, collecting every value; without a `.close` it would wait forever.' },

  { part: 5, topic: 'Supplies and react', type: 'multi',
    q: 'Which of the following are true of a live supply created from a `Supplier` (as opposed to an on-demand supply like `Supply.from-list`)?',
    options: ['`A tap added after a value is emitted misses that earlier value`', '`Each new tap replays the whole sequence from the start`', '`Values are pushed to taps with .emit on the Supplier`', '`It is the right model for real events such as clicks or messages`'],
    answer: [0, 2, 3],
    explain: 'A live supply broadcasts to current taps only and does not remember past values; replaying to every tap is on-demand behaviour.' },

  { part: 5, topic: 'Cro / web', type: 'choice',
    q: 'In a Cro `route` block, which function sets both the content type and the response body, so you never write a status line or `\\r\\n` by hand?',
    code: "get -> 'hello' {\n    ______ 'text/plain', 'Hello from Cro!';\n}",
    options: ['`respond`', '`content`', '`emit`', '`print`'],
    answer: 1,
    explain: 'The `content` function sets the content type and body of a Cro response.' }
];
