---
title: Quiz — Protocollum iteratorum
translations_gpt:
---

{% include menu.html %}

Cum iterator nullos valores amplius dare habet, quid `pull-one` reddit, et quomodo id probas?

{:.quiz}
1 | `IterationEnd`, per `=:=` probatum
0 | `Nil`, per `.defined` probatum
0 | indicem vacuum `()`, per `==` probatum
0 | `0`, per `=:=` probatum

{% include quiz.html %}

<div class="extended-explanation">

Iterator exhaustus obiectum signi `IterationEnd` reddit. Quia unum obiectum commune est, illud per identitatem cum `=:=` inspicis (probatio *eiusdem obiecti*), non per valorem vel definitionem. Anulus `for` hac ipsa probatione utitur ut sciat quando consistat.

</div>

{% include nav.html %}
