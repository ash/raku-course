---
title: Примените дважды
translations_gpt:
---

{% include menu.html %}

## Задача

Напишите подпрограмму `twice`, которая принимает подпрограмму `&f` и значение `$x` и применяет
`&f` к `$x` **дважды** (то есть `f(f($x))`). Проверьте её, утроив `2` дважды.

## Пример

Программа печатает:

```
18
```

## Решение

✅ [Посмотреть решение](solution)

{% include nav.html %}
