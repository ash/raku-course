---
title: Поглощающие именованные аргументы
translations_gpt:
---

{% include menu.html %}

## Задача

Напишите подпрограмму `describe`, которая принимает обязательное позиционное имя, а затем собирает любое количество именованных аргументов в поглощающий хеш (`*%opts`). Она должна возвращать строку вида `name: key=value, key=value, ...`, перечисляя все опции с ключами в алфавитном порядке.

Вызовите её как `describe('Anna', colour => 'red', size => 5)` и выведите результат.

## Пример

Программа выводит:

```
Anna: colour=red, size=5
```

## Решение

✅ [Смотреть решение](solution)

{% include nav.html %}
