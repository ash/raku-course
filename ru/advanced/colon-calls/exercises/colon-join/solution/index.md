---
title: 'Решение: join через двоеточие'
translations_gpt: Решение задачи «Соединение с двоеточием»
---

{% include menu.html %}

Вот возможное решение задачи.

## Код

```raku
say <a b c>.reverse.join: '-';
```

🦋 Исходный код можно найти в файле [colon-join.raku](https://github.com/ash/raku-course/blob/master/exercises/advanced/colon-calls/colon-join.raku).

## Вывод

```
c-b-a
```

## Комментарии

1. `.reverse` остаётся в обычной форме, поскольку стоит в середине цепочки. `join` идёт последним, поэтому может использовать двоеточие, передавая `'-'` точно так же, как `join('-')`.

1. Развёрнутый список `c, b, a` затем соединяется дефисами, давая `c-b-a`. Форма с двоеточием работает с обычными значениями, а не только с блоками.

{% include nav.html %}
