---
title: Whatever с map
translations_gpt:
---

{% include menu.html %}

## Задача

`WhateverCode` — это обычное значение-функция, поэтому его можно сохранить и передать в `map`. Создайте функцию с одним аргументом, которая утраивает свой аргумент, сохраните её в `$triple` и используйте для обработки списка `1..4` с помощью `map`. Выведите результат.

## Пример

Программа выводит:

```
(3 6 9 12)
```

## Решение

✅ [Смотрите решение](solution)

{% include nav.html %}
