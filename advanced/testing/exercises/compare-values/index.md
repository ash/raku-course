---
title: Exercise ’Compare values‘
---

{% include menu.html %}

## Problem

Using the `Test` module’s `is` function, check that `'a'.uc` equals `'A'`, with the description `uppercase`. Finish with `done-testing`.

## Example

The program prints:

```
ok 1 - uppercase
1..1
```

## Solution

✅ [See the solution](solution)

{% include nav.html %}
