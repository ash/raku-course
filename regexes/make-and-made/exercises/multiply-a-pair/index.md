---
title: Exercise ’Multiply a pair‘
---

{% include menu.html %}

## Problem

Write a grammar that parses two numbers joined by a `*`, as in `'4*5'`, and uses an inline `make` to store their **product**. Parse `'4*5'` and print `.made`.

## Example

The program prints:

```
20
```

## Solution

✅ [See the solution](solution)

{% include nav.html %}
