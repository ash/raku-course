---
title: Вправа «Фігура та квадрат»
translations_gpt:
---

{% include menu.html %}

## Завдання

Визначте базовий клас `Shape` із методом `name`, який повертає `'shape'`, і методом `describe`, що повертає рядок `I am a <name>` (використовуючи власний `name`).

Потім визначте клас `Square`, який успадковує від `Shape` і перевизначає `name`, щоб той повертав `'square'`.

Виведіть опис об'єкта `Square`.

## Приклад

Програма виводить:

```
I am a square
```

## Рішення

✅ [Дивіться рішення](solution)

{% include nav.html %}
