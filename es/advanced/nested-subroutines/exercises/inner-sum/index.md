---
title: Una suma interna
translations_gpt:
---

{% include menu.html %}

## Problema

Escribe una subrutina `compute` que defina una función auxiliar anidada `add`, la cual devuelva la suma de sus dos argumentos. Dentro de `compute`, devuelve `add(2, 3) + add(4, 5)`.

Imprime el resultado de llamar a `compute`.

## Ejemplo

El programa imprime:

```
14
```

## Solución

✅ [Ver la solución](solution)

{% include nav.html %}
