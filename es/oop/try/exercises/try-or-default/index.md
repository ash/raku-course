---
title: Intento o valor por defecto
translations_gpt:
---

{% include menu.html %}

## Problema

Un bloque `try` que falla vale un valor no definido. Aprovéchalo con el operador defined-or `//` para aportar un valor de reserva: ejecuta un bloque `try` que haga `die 'no'` y, si falla, usa en su lugar la cadena `default`. Imprime el resultado.

## Ejemplo

El programa imprime:

```
default
```

## Solución

✅ [Ver la solución](solution)

{% include nav.html %}
