---
title: Una palabra invertida
translations_gpt:
---

{% include menu.html %}

## Problema

Define una clase `Word` con un atributo `text` y un método `reversed` que devuelva el texto de la palabra escrito al revés (el método `flip` invierte una cadena).

Crea una palabra que contenga `Raku` e imprime su inversión.

## Ejemplo

El programa imprime:

```
ukaR
```

## Solución

✅ [Ver la solución](solution)

{% include nav.html %}
