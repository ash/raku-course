---
title: Regex frente a token
translations_gpt:
---

{% include menu.html %}

## Problema

Muestra la diferencia que marca el retroceso. Declara el **mismo** patrón — uno o más dígitos seguidos de un `5` literal — dos veces: una como `regex` y otra como `token`. Empareja cada uno contra `'12345'` e imprime un `True` o `False` simple para cada uno.

## Ejemplo

El programa imprime:

```
True
False
```

## Solución

✅ [Ver la solución](solution)

{% include nav.html %}
