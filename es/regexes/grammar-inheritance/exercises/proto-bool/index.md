---
title: Un booleano con proto
translations_gpt:
---

{% include menu.html %}

## Problema

Escribe una gramática que analice un `bool`, que es o bien la palabra `true` o bien la palabra `false`. Usa un **proto token** con dos variantes `:sym<…>`. Analiza `'true'`, `'false'` y también `'unknown'` (que no es ninguna de las dos), imprimiendo si cada análisis funcionó.

## Ejemplo

El programa imprime:

```
True
True
False
```

## Solución

✅ [Ver la solución](solution)

{% include nav.html %}
