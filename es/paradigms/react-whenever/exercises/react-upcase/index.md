---
title: Mayúsculas con react
translations_gpt:
---

{% include menu.html %}

## Problema

Usando un bloque `react` con un solo `whenever`, recoge los valores de un supply construido a partir de las cadenas `'a'`, `'b'`, `'c'`, añadiendo cada una **en mayúsculas** a un array. Imprime el array después de que el bloque react termine.

## Ejemplo

El programa imprime:

```
[A B C]
```

## Solución

✅ [Ver la solución](solution)

{% include nav.html %}
