---
title: Exercise ’Drain a channel‘
---

{% include menu.html %}

## Problem

Create a channel, send the strings `'a'` and `'b'`, close it, then collect all the values with `.list` and print them joined by commas.

## Example

The program prints:

```
a,b
```

## Solution

✅ [See the solution](solution)

{% include nav.html %}
