---
title: Exercise ’Apply twice‘
---

{% include menu.html %}

## Problem

Write a subroutine `twice` that takes a subroutine `&f` and a value `$x`, and applies `&f` to `$x` **twice** (that is, `f(f($x))`). Test it by tripling `2` twice.

## Example

The program prints:

```
18
```

## Solution

✅ [See the solution](solution)

{% include nav.html %}
