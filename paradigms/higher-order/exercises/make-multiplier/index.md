---
title: Exercise ’Make a multiplier‘
---

{% include menu.html %}

## Problem

Write a subroutine `multiplier` that takes a number `$n` and **returns** a subroutine which multiplies its argument by `$n`. Use it to build two functions — a “double” and a “triple” — then call each with `7`.

## Example

The program prints:

```
14
21
```

## Solution

✅ [See the solution](solution)

{% include nav.html %}
