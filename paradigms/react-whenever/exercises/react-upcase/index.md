---
title: Exercise ’Upper-case with react‘
---

{% include menu.html %}

## Problem

Using a `react` block with a single `whenever`, collect the values of a supply built from the strings `'a'`, `'b'`, `'c'`, pushing each one **upper-cased** into an array. Print the array after the react block finishes.

## Example

The program prints:

```
[A B C]
```

## Solution

✅ [See the solution](solution)

{% include nav.html %}
