// AUTO-GENERATED by rakujs/gen-examples.raku — do not edit.
// Source of truth: examples/*.raku
window.RAKU_EXAMPLES = [
{"name": "Hello, world", "category": "Basics", "code": "say \"Hello, Raku from the browser!\";"},
{"name": "Ranges & map", "category": "Basics", "code": "say (1..10).map(* ** 2).join(\", \");"},
{"name": "FizzBuzz", "category": "Basics", "code": "for 1..20 {\n    when $_ %% 15 { say \"FizzBuzz\" }\n    when $_ %%  3 { say \"Fizz\" }\n    when $_ %%  5 { say \"Buzz\" }\n    default        { say $_ }\n}"},
{"name": "mandel", "category": "Graphics & fractals", "desc": "Print the Mandelbrot set.", "code": "#!/usr/bin/env raku\n# Print the Mandelbrot set.\n#\n# History:\n#   Originally languages/perl6/examples/mandel.p6 in the Parrot project\n#   (~2004), translated from C by Glenn Rhoads and ported to Perl 6 by\n#   Leopold Tötsch <lt@toetsch.at>. That file no longer compiles under\n#   present-day Raku (nor under raku++) because of two language changes\n#   since the Parrot era; see docs/MANDEL.md for the full story.\n#\n# This is a faithful modernisation. Only two things changed, both forced\n# by the language rather than by any single implementation:\n#   1. Whitespace is now required around the  <  and  >  comparison\n#      operators. The original wrote  $k<112 ; today  $k<...>  is\n#      angle-bracket subscript/word-quote syntax, so it must be  $k < 112.\n#   2. A C-style comma expression used as a loop condition is now a List\n#      (always true) instead of \"the value of the last element\". The\n#      per-iteration side effects that used to live in the condition are\n#      moved into the loop init / body / step.\n#\n# The algorithm, the character ramp, and the 75x30 geometry are unchanged.\n# Output is byte-for-byte identical under Rakudo and raku++.\n\nsub MAIN() {\n    my ($x, $y, $k);\n    my @b = (' ', '.', ':', ',', ';', '!', '/', '>', ')', '|', '&', 'I', 'H', '%', '*', '#');\n\n    my ($r, $i, $z, $Z, $t, $c, $C);\n    loop ($y = 30; $y > 0; $y--) {\n        $C = $y * 0.1 - 1.5;\n        loop ($x = 0; $x < 75; $x++) {\n            $c = $x * 0.04 - 2.0;\n            $z = 0.0;\n            $Z = 0.0;\n            loop ($r = $c, $i = $C, $k = 0; $k < 112; $k++) {\n                $t = $z * $z - $Z * $Z + $r;\n                $Z = 2.0 * $z * $Z + $i;\n                $z = $t;\n                last if $z * $z + $Z * $Z > 10.0;\n            }\n            print @b[$k % 16];\n        }\n        print \"\\n\";\n    }\n}\n\n"},
{"name": "sierpinski", "category": "Graphics & fractals", "desc": "The Sierpinski triangle, drawn by an elementary cellular automaton.", "code": "#!/usr/bin/env raku\n# The Sierpinski triangle, drawn by an elementary cellular automaton.\n#\n# Rule 90 is the simplest rule that produces a fractal: each cell in the\n# next row is the exclusive-or (+^) of its two upper neighbours. Start\n# from a single live cell in the middle of an otherwise empty row and let\n# the rule run, and the live cells trace out the Sierpinski gasket — the\n# same self-similar triangle you get from Pascal's triangle taken mod 2.\n#\n# Features: infix bit ops (`+^`), array building, `.map`/`.join` for the\n# row-to-text step. No randomness; the picture is identical every run.\n\nmy $rows  = 32;\nmy $width = 2 * $rows - 1;      # room for the triangle to spread each side\n\n# One row of cells: 0 = dead, 1 = live. Seed a single live cell in the\n# centre.\nmy @row = 0 xx $width;\n@row[$width div 2] = 1;\n\nfor ^$rows {\n    # Draw the current generation. '#' is a live cell, a space is a dead one.\n    say @row.map({ $_ ?? '#' !! ' ' }).join;\n\n    # Rule 90: next[i] = row[i-1] +^ row[i+1], with the world padded by\n    # dead cells beyond both edges.\n    my @next;\n    for ^$width -> $i {\n        my $left  = $i > 0          ?? @row[$i - 1] !! 0;\n        my $right = $i < $width - 1  ?? @row[$i + 1] !! 0;\n        @next[$i] = $left +^ $right;\n    }\n    @row = @next;\n}\n"},
{"name": "life", "category": "Graphics & fractals", "desc": "Conway's Game of Life on a toroidal grid, rendered as ASCII frames.", "code": "#!/usr/bin/env raku\n# Conway's Game of Life on a toroidal grid, rendered as ASCII frames.\n#\n# A cell lives into the next generation if it has 2 or 3 live neighbours;\n# a dead cell with exactly 3 live neighbours is born. The board wraps around\n# at the edges (a torus). The starting field is a fresh random \"soup\" every\n# run — each cell alive with probability DENSITY — so no two runs evolve the\n# same way; watch the population settle into still lifes and blinkers.\n\nconstant \\W = 30;\nconstant \\H = 16;\nconstant \\GENERATIONS = 100;\nconstant \\DENSITY = 0.28;\n\n# Seed: a random soup. `xx` re-evaluates `rand` for every cell, and builds\n# H independent rows.\nmy @grid = [ [ (rand < DENSITY) xx W ] xx H ];\n\nsub neighbours(@g, $r, $c) {\n    my $n = 0;\n    for -1, 0, 1 -> $dr {\n        for -1, 0, 1 -> $dc {\n            next if $dr == 0 && $dc == 0;\n            $n++ if @g[($r + $dr) % H][($c + $dc) % W];\n        }\n    }\n    $n;\n}\n\nsub step(@g) {\n    my @next = [ [False xx W] xx H ];\n    for ^H -> $r {\n        for ^W -> $c {\n            my $n = neighbours(@g, $r, $c);\n            @next[$r][$c] = @g[$r][$c] ?? (2 <= $n <= 3) !! ($n == 3);\n        }\n    }\n    @next;\n}\n\n# Repaint a frame in place. `\\e[H` moves the cursor back to the top-left\n# instead of scrolling, so each generation overwrites the previous one. Every\n# row is full width, so nothing from the old frame is ever left behind.\nsub show(@g, $gen) {\n    my $alive = @g.map({ .grep(?*).elems }).sum;\n    print \"\\e[H\";\n    say \"generation $gen ($alive alive)    \";\n    for ^H -> $r {\n        say @g[$r].map({ $_ ?? '#' !! '.' }).join;\n    }\n}\n\nsub MAIN(Real :$delay = 0.08) {\n    print \"\\e[2J\\e[?25l\";          # clear the screen, hide the cursor\n    LEAVE print \"\\e[?25h\\n\";       # restore the cursor on the way out\n    for 0 ..^ GENERATIONS -> $gen {\n        show(@grid, $gen);\n        @grid = step(@grid);\n        sleep $delay;\n    }\n}\n"},
{"name": "calculator", "category": "Grammars & parsing", "desc": "A four-function calculator built from a Raku grammar and an actions class.", "code": "#!/usr/bin/env raku\n# A four-function calculator built from a Raku grammar and an actions class.\n#\n# The grammar describes the shape of an arithmetic expression; the actions\n# class turns each successful match into a number as it is reduced, so\n# precedence and associativity fall out of the grammar structure itself\n# rather than from a hand-written parser. `%` is the separator meta-operator:\n# `<term>+ % <op>` means \"one or more terms separated by operators\".\n\ngrammar Expr {\n    rule  TOP     { <sum> }\n    rule  sum     { <product>+ % <add-op> }\n    rule  product { <factor>+  % <mul-op> }\n    rule  factor  { <number> | '(' <sum> ')' }\n    token number  { '-'? \\d+ ['.' \\d+]? }\n    token add-op  { '+' | '-' }\n    token mul-op  { '*' | '/' }\n}\n\nsub fold(@vals, @ops) {\n    my $acc = @vals[0];\n    for @ops.kv -> $i, $op {\n        given $op {\n            when '+' { $acc += @vals[$i + 1] }\n            when '-' { $acc -= @vals[$i + 1] }\n            when '*' { $acc *= @vals[$i + 1] }\n            when '/' { $acc /= @vals[$i + 1] }\n        }\n    }\n    $acc;\n}\n\nclass Calc {\n    method TOP     ($/) { make $<sum>.made }\n    method factor  ($/) { make $<number> ?? +$<number> !! $<sum>.made }\n    method product ($/) { make fold($<factor>».made, $<mul-op>».Str) }\n    method sum     ($/) { make fold($<product>».made, $<add-op>».Str) }\n}\n\nsub MAIN() {\n    my @tests =\n        '1 + 2 * 3',\n        '(1 + 2) * 3',\n        '10 / 4',                 # stays an exact Rat (5/2), never a float\n        '2 * (3 + 4) - 5 / 2',\n        '-3 + 4 * -2';\n\n    for @tests -> $src {\n        my $result = Expr.parse($src, actions => Calc).made;\n        say sprintf('%-22s = %s', $src, $result);\n    }\n}\n"},
{"name": "json", "category": "Grammars & parsing", "desc": "A JSON parser written as a Raku grammar plus an actions class.", "code": "#!/usr/bin/env raku\n# A JSON parser written as a Raku grammar plus an actions class.\n#\n# The grammar describes JSON's shape; the actions class turns each match into\n# a native Raku value as the parse reduces, so a JSON object becomes a Hash, an\n# array becomes an Array, and the scalars become Str / Int / Rat / Bool / Any.\n# `proto rule value {*}` with `:sym<...>` alternatives gives one method per JSON\n# value kind. Integers stay Int and decimals become an exact Rat, not a float.\n\ngrammar JSON {\n    rule  TOP              { <value> }\n    proto rule value       {*}\n    rule  value:sym<object> { '{' <pairs> '}' }\n    rule  value:sym<array>  { '[' <items> ']' }\n    token value:sym<str>    { <str> }\n    token value:sym<num>    { '-'? \\d+ ['.' \\d+]? }\n    token value:sym<true>   { 'true' }\n    token value:sym<false>  { 'false' }\n    token value:sym<null>   { 'null' }\n    rule  pairs             { <pair>* % ',' }\n    rule  pair              { <str> ':' <value> }\n    rule  items             { <value>* % ',' }\n    token str               { '\"' ( <-[\"]>* ) '\"' }\n}\n\nclass Actions {\n    method TOP($/)               { make $<value>.made }\n    method value:sym<object>($/) { make $<pairs>.made }\n    method value:sym<array>($/)  { make $<items>.made }\n    method value:sym<str>($/)    { make $<str>.made }\n    method value:sym<num>($/)    { make( (~$/).contains('.') ?? (~$/).Rat !! (~$/).Int ) }\n    method value:sym<true>($/)   { make True }\n    method value:sym<false>($/)  { make False }\n    method value:sym<null>($/)   { make Any }\n    method pairs($/)             { make $<pair>.map({ .made }).hash }\n    method pair($/)              { make $<str>.made => $<value>.made }\n    method items($/)             { make $<value>.map({ .made }).Array }\n    method str($/)               { make ~$0 }\n}\n\nsub type-of($v) {\n    return 'Null'  unless $v.defined;\n    return 'Bool'  if $v ~~ Bool;\n    return 'Int'   if $v ~~ Int;\n    return 'Rat'   if $v ~~ Rat;\n    return 'Str'   if $v ~~ Str;\n    return 'Array' if $v ~~ Positional;\n    return 'Hash'  if $v ~~ Associative;\n    $v.^name;\n}\n\n# Walk the parsed structure and print it indented, annotating each scalar with\n# the Raku type it became — proof that this is a live data structure, not text.\nsub dump($v, $indent = 0) {\n    my $pad = '  ' x $indent;\n    if $v ~~ Associative {\n        for $v.keys.sort -> $k {\n            my $child = $v{$k};\n            if $child ~~ Associative | Positional {\n                say \"$pad$k:\";\n                dump($child, $indent + 1);\n            }\n            else {\n                say \"$pad$k: $child.gist() ({type-of($child)})\";\n            }\n        }\n    }\n    elsif $v ~~ Positional {\n        for $v.kv -> $i, $child {\n            if $child ~~ Associative | Positional {\n                say \"$pad\\[$i]:\";\n                dump($child, $indent + 1);\n            }\n            else {\n                say \"$pad\\[$i] $child.gist() ({type-of($child)})\";\n            }\n        }\n    }\n}\n\nsub MAIN() {\n    my $json = q:to/END/;\n    {\n        \"mission\": \"Voyager 1\",\n        \"launched\": 1977,\n        \"active\": true,\n        \"distance_au\": 163.9,\n        \"instruments\": [\"ISS\", \"PLS\", \"MAG\"],\n        \"target\": null,\n        \"trajectory\": { \"escaped\": true, \"speed_km_s\": 17.0 }\n    }\n    END\n\n    my %data = JSON.parse($json.trim, actions => Actions).made;\n\n    say 'Parsed JSON into a live Raku structure:';\n    dump(%data);\n    say '';\n\n    # Query the structure the way you would any Raku Hash/Array.\n    say \"mission is a {type-of(%data<mission>)}: %data<mission>\";\n    say \"first instrument: %data<instruments>[0]\";\n    say \"instrument count: {%data<instruments>.elems}\";\n    say \"distance stayed exact: {%data<distance_au>.nude.join('/')}\";\n    say \"nested speed: {%data<trajectory><speed_km_s>} ({type-of(%data<trajectory><speed_km_s>)})\";\n}\n"},
{"name": "rpn", "category": "Grammars & parsing", "desc": "A Reverse Polish Notation (postfix) calculator driven by an explicit stack.", "code": "#!/usr/bin/env raku\n# A Reverse Polish Notation (postfix) calculator driven by an explicit stack.\n#\n# Postfix needs no parentheses and no precedence rules: read left to right,\n# push every number, and when an operator turns up pop its two operands,\n# combine them, and push the result back. An ordinary Array used as a stack\n# (`push`/`pop`) is the whole machine. Arithmetic stays exact — dividing two\n# integers yields a `Rat`, so 15 / 5 is 3 and 1 / 3 is a true one-third.\n\nsub rpn(Str $expr) {\n    my @stack;\n    for $expr.words -> $tok {\n        if $tok ~~ /^ '-'? \\d+ ['.' \\d+]? $/ {\n            @stack.push: $tok.contains('.') ?? $tok.Rat !! $tok.Int;\n        }\n        else {\n            # Operator: pop the right operand first, then the left.\n            my $b = @stack.pop;\n            my $a = @stack.pop;\n            given $tok {\n                when '+' { @stack.push: $a + $b }\n                when '-' { @stack.push: $a - $b }\n                when '*' { @stack.push: $a * $b }\n                when '/' { @stack.push: $a / $b }\n                default  { die \"unknown operator '$tok'\" }\n            }\n        }\n    }\n    @stack.pop;   # the sole survivor is the answer\n}\n\nsub fmt($n) {\n    # Show exact fractions as numerator/denominator, integers plainly.\n    return ~$n if $n ~~ Int;\n    $n.denominator == 1 ?? ~$n.numerator !! $n.nude.join('/');\n}\n\nsub MAIN() {\n    my @tests =\n        '3 4 +',                 # 7\n        '3 4 + 5 *',             # 35\n        '5 1 2 + 4 * + 3 -',     # 14\n        '15 7 1 1 + - / 3 *',    # 9\n        '2 3 4 * +',             # 14\n        '10 2 /',                # 5\n        '1 3 /',                 # exact 1/3, not 0.333...\n        '100 5 / 3 -';           # 17\n\n    for @tests -> $expr {\n        say sprintf('%-22s = %s', $expr, fmt(rpn($expr)));\n    }\n}\n"},
{"name": "primes", "category": "Numbers & maths", "desc": "A short tour of number theory, leaning on Raku's arbitrary-precision", "code": "#!/usr/bin/env raku\n# A short tour of number theory, leaning on Raku's arbitrary-precision\n# integers (there is no 64-bit ceiling — factorials and Mersenne numbers\n# grow to whatever size they need) and the lazy sequence operator `...`.\n\n# Sieve of Eratosthenes as a lazy, self-referential sequence: each new\n# prime is the next number not divisible by any prime already found.\nmy @primes = lazy gather {\n    my @seen;\n    my $n = 2;\n    loop {\n        unless @seen.first: $n %% * {\n            @seen.push: $n;\n            take $n;\n        }\n        $n++;\n    }\n}\n\nsay 'First 20 primes:';\nsay @primes[^20];\n\nsay '';\nsay 'Primes between 100 and 130:';\nsay @primes[^40].grep({ 100 <= $_ <= 130 });\n\n# Arbitrary precision: factorials well past what a machine int could hold.\nsay '';\nsay 'Factorials:';\nfor 10, 25, 50 -> $n {\n    say \"  $n! = \", [*] 1 .. $n;\n}\n\n# Mersenne primes: numbers of the form 2^p - 1 that are themselves prime.\nsay '';\nsay 'Mersenne primes 2^p - 1 (p prime, p < 32):';\nfor @primes[^11] -> $p {\n    my $m = 2 ** $p - 1;\n    say \"  p=$p  2^{$p}-1 = $m\" if $m.is-prime;\n}\n\n# The Collatz (3n+1) sequence: the stopping time for a few seeds.\nsub collatz-steps($start) {\n    my $n = $start;\n    my $steps = 0;\n    while $n != 1 {\n        $n = $n %% 2 ?? $n div 2 !! 3 * $n + 1;\n        $steps++;\n    }\n    $steps;\n}\n\nsay '';\nsay 'Collatz stopping times:';\nfor 6, 27, 97 -> $n {\n    say \"  $n reaches 1 in {collatz-steps($n)} steps\";\n}\n"},
{"name": "rationals", "category": "Numbers & maths", "desc": "Exact rational arithmetic. In Raku a decimal literal like 0.1 is a `Rat`", "code": "#!/usr/bin/env raku\n# Exact rational arithmetic. In Raku a decimal literal like 0.1 is a `Rat`\n# (a numerator/denominator pair), not a floating-point approximation, so\n# sums that famously go wrong in binary floating point stay exact here.\n\nsay 'Floating-point folklore, done exactly:';\nsay \"  0.1 + 0.2        = {0.1 + 0.2}\";          # 0.3, not 0.30000000000000004\nsay \"  0.1 + 0.2 - 0.3  = {0.1 + 0.2 - 0.3}\";    # exactly 0\nsay \"  (0.1 + 0.2).nude = {(0.1 + 0.2).nude.join('/')}\";  # 3/10 — a real Rat\nsay '';\n\n# The harmonic numbers H_n = 1 + 1/2 + 1/3 + ... + 1/n are rationals whose\n# denominators blow up fast. With a real bignum tower they stay exact.\nsay 'Harmonic numbers H_n (exact):';\nmy $h = 0;\nfor 1 .. 10 -> $n {\n    $h += 1 / $n;\n    say \"  H_$n = {$h.nude.join('/')}\" if $n == any(1, 5, 10);\n}\nsay '';\n\n# Continued-fraction convergents of pi: each is the best rational\n# approximation of pi for its denominator size. 355/113 is famous for\n# being accurate to seven digits.\nsay 'Rational approximations of pi from its continued fraction:';\nmy @cf = 3, 7, 15, 1, 292, 1, 1;\nfor 1 .. @cf -> $len {\n    my ($num, $den) = 1, 0;\n    for @cf[^$len].reverse -> $a {\n        ($num, $den) = $a * $num + $den, $num;\n    }\n    my $approx = $num / $den;\n    say sprintf('  %-9s = %.10f  (error %.2e)',\n                \"$num/$den\", $approx, abs($approx - pi));\n}\nsay '';\n\n# Any rational round-trips exactly through its own continued fraction.\nsub to-cf(Rat $r is copy) {\n    my @terms;\n    loop {\n        @terms.push: $r.floor;\n        my $frac = $r - $r.floor;\n        last if $frac == 0;\n        $r = 1 / $frac;\n    }\n    @terms;\n}\n\nmy $x = 355/113;\nsay \"Continued fraction of {$x.nude.join('/')} is \", to-cf($x).raku;\n"},
{"name": "fibonacci", "category": "Numbers & maths", "desc": "Three ways to reach the Fibonacci numbers, and a check that they agree.", "code": "#!/usr/bin/env raku\n# Three ways to reach the Fibonacci numbers, and a check that they agree.\n# Shows off lazy self-referential sequences, a fast-doubling recursion that\n# reaches deep into the sequence in a handful of steps, and Raku's\n# arbitrary-precision integers (the 500th Fibonacci number has 105 digits).\n\n# (a) The whole sequence as a lazy, self-referential list: each term is the\n# sum of the previous two, and `... *` lets it run forever without computing\n# more than you ask for.\nmy @fib = 1, 1, * + * ... *;\n\nsay 'The lazy sequence, first 16 terms:';\nsay @fib[^16];\nsay '';\n\n# (b) Fast doubling: from (F(n), F(n+1)) you can jump straight to\n# (F(2n), F(2n+1)), so F(n) costs only about log2(n) steps of big-integer\n# arithmetic instead of n. Returns the pair (F(n), F(n+1)).\nsub fib-pair($n) {\n    return (0, 1) if $n == 0;\n    my ($a, $b) = fib-pair($n div 2);\n    my $c = $a * (2 * $b - $a);       # F(2k)   = F(k) * (2 F(k+1) - F(k))\n    my $d = $a * $a + $b * $b;        # F(2k+1) = F(k)^2 + F(k+1)^2\n    $n %% 2 ?? ($c, $d) !! ($d, $c + $d);\n}\nsub fib($n) { fib-pair($n)[0] }\n\n# (c) The two methods must agree. @fib is 1-indexed against the usual F(n)\n# (F(1) = F(2) = 1), so @fib[n-1] is F(n).\nsay 'Lazy list and fast doubling agree for F(1)..F(30):';\nmy $agree = True;\nfor 1 .. 30 -> $n {\n    $agree = False unless @fib[$n - 1] == fib($n);\n}\nsay \"  \", $agree ?? 'all 30 match' !! 'MISMATCH';\nsay '';\n\n# Arbitrary precision: no 64-bit ceiling, so these print in full.\nsay 'Big Fibonacci numbers, in full:';\nfor 100, 250, 500 -> $n {\n    my $f = fib($n);\n    say \"  F($n) has {$f.Str.chars} digits: $f\";\n}\nsay '';\n\n# Consecutive ratios F(n+1)/F(n) close in on the golden ratio.\nsay 'Ratios F(n+1)/F(n) approach the golden ratio phi:';\nmy $phi = (1 + 5.sqrt) / 2;\nfor 5, 10, 20, 40 -> $n {\n    my $ratio = fib($n + 1) / fib($n);\n    say sprintf('  F(%d+1)/F(%d) = %.12f   (phi - ratio = %.2e)',\n                $n, $n, $ratio, $phi - $ratio);\n}\nsay sprintf('  phi        = %.12f', $phi);\n"},
{"name": "factorize", "category": "Numbers & maths", "desc": "A little number-theory workshop: prime factorization, divisor counts, and", "code": "#!/usr/bin/env raku\n# A little number-theory workshop: prime factorization, divisor counts, and\n# the two classic partners gcd and lcm. Shows trial-division factoring, hash\n# tallies for exponents, and Raku's built-in `gcd` / `lcm` infix operators\n# (which also work as reductions, `[gcd]` / `[lcm]`).\n\n# Trial division: pull out each prime factor and tally its exponent in a hash.\nsub prime-factors($n is copy) {\n    my %exp;\n    my $d = 2;\n    while $d * $d <= $n {\n        while $n %% $d {\n            %exp{$d}++;\n            $n = $n div $d;\n        }\n        $d++;\n    }\n    %exp{$n}++ if $n > 1;        # whatever is left is a prime\n    %exp;\n}\n\n# Render 360 as \"2^3 * 3^2 * 5\" (a lone prime keeps no exponent).\nsub factor-string(%exp) {\n    return '1' unless %exp;\n    %exp.keys.sort({ $^a <=> $^b }).map(-> $p {\n        %exp{$p} == 1 ?? \"$p\" !! \"$p^%exp{$p}\"\n    }).join(' * ');\n}\n\nsay 'Prime factorizations (with divisor counts):';\nfor 360, 97, 1024, 5040, 600851475143 -> $n {\n    my %e = prime-factors($n);\n    # The number of divisors is the product of (exponent + 1) over all primes.\n    my $divisors = [*] %e.values.map(* + 1);\n    $divisors = 1 unless %e;\n    say sprintf('  %-14d = %-22s  %d divisors',\n                $n, factor-string(%e), $divisors);\n}\nsay '';\n\n# gcd and lcm as infix operators. gcd via Euclid is a one-liner too, shown\n# here as a check that the built-in agrees.\nsub my-gcd($a is copy, $b is copy) {\n    ($a, $b) = $b, $a % $b while $b;\n    $a;\n}\n\nsay 'gcd and lcm (built-in infix vs. Euclid by hand):';\nfor (48, 36), (1071, 462), (17, 5) -> ($a, $b) {\n    my $g = $a gcd $b;\n    my $l = $a lcm $b;\n    my $check = my-gcd($a, $b) == $g ?? 'ok' !! 'MISMATCH';\n    say \"  $a, $b:  gcd = $g  lcm = $l   (Euclid check: $check)\";\n}\nsay '';\n\n# Reductions fold the operator across a whole list.\nsay 'Reductions across a list:';\nmy @nums = 24, 60, 36, 90;\nsay \"  numbers      = @nums[]\";\nsay \"  [gcd] @nums[] = \", [gcd] @nums;\nsay \"  [lcm] @nums[] = \", [lcm] @nums;\n"},
{"name": "pascal", "category": "Numbers & maths", "desc": "Pascal's triangle, and two things it secretly encodes.", "code": "#!/usr/bin/env raku\n# Pascal's triangle, and two things it secretly encodes.\n#\n# Shows off list building (each row grows from the one above by adding\n# neighbours with the zip operator `Z+`), combinatorics, and a little\n# ASCII formatting. Every entry is an exact arbitrary-precision `Int`.\n\nmy $rows = 12;\n\n# Build the triangle. The next row is 1, the pairwise sums of the current\n# row's neighbours, then 1 — which is exactly what `Z+` (zip-with-plus)\n# gives when a row is added to itself shifted by one.\nmy @triangle;\nmy @row = 1;\nfor ^$rows {\n    @triangle.push: @row;\n    @row = 1, |(@row Z+ @row[1 .. *]), 1;\n}\n\n# Print it centred. Each entry sits in a fixed-width cell, and every row\n# is indented by half a cell so the triangle stays symmetric.\nmy $cell = @triangle[*-1].map(*.chars).max + 1;\nsay \"Pascal's triangle:\";\nfor @triangle -> @r {\n    my $indent = ' ' x ($cell * ($rows - @r.elems) div 2);\n    say $indent ~ @r.map({ sprintf('%*d', $cell, $_) }).join;\n}\n\n# The entry in row n, position k is the binomial coefficient \"n choose k\".\n# Compute one directly and check it against the triangle we built.\nsub choose($n, $k) { ([*] $n - $k + 1 .. $n) div [*] 1 .. $k }\n\nsay '';\nsay 'Row 11 is the binomial coefficients C(11, k):';\nsay '  from triangle: ', @triangle[11];\nsay '  from C(11, k): ', (0 .. 11).map({ choose(11, $_) });\nsay '  match: ', @triangle[11] eqv (0 .. 11).map({ choose(11, $_) }).list;\n\n# Colour each entry by parity: the odd numbers alone trace out the\n# Sierpinski triangle, a fractal hiding inside the arithmetic.\nsay '';\nsay 'Odd entries only (the Sierpinski triangle):';\nfor @triangle -> @r {\n    my $indent = ' ' x ($rows - @r.elems);\n    say $indent ~ @r.map({ $_ % 2 ?? '#' !! ' ' }).join(' ');\n}\n"},
{"name": "matrix", "category": "Numbers & maths", "desc": "Linear algebra by hand on arrays-of-arrays: multiply, transpose, an", "code": "#!/usr/bin/env raku\n# Linear algebra by hand on arrays-of-arrays: multiply, transpose, an\n# identity matrix, and a 3x3 determinant. No library — just nested loops\n# and reductions. Everything stays exact `Int`, so the answers are precise.\n\n# Pretty-print a matrix with right-aligned, fixed-width columns.\nsub show(@m, $label) {\n    my $cell = @m.map(*.map(*.chars).max).max + 1;\n    say $label;\n    for @m -> @row {\n        say '  ' ~ @row.map({ sprintf('%*d', $cell, $_) }).join;\n    }\n}\n\n# Matrix product: entry (i, j) is the dot product of row i of A with\n# column j of B. The inner `[+]` reduces the pairwise products to a sum.\nsub multiply(@a, @b) {\n    my @out;\n    for ^@a.elems -> $i {\n        my @row;\n        for ^@b[0].elems -> $j {\n            @row.push: [+] (^@b.elems).map({ @a[$i][$_] * @b[$_][$j] });\n        }\n        @out.push: @row;\n    }\n    @out;\n}\n\n# Transpose: swap rows and columns.\nsub transpose(@m) {\n    my @out;\n    for ^@m[0].elems -> $j {\n        my @col;\n        for @m -> @row {\n            @col.push: @row[$j];\n        }\n        @out.push: @col;\n    }\n    @out;\n}\n\n# An n-by-n identity matrix: 1 on the diagonal, 0 elsewhere.\nsub identity($n) {\n    my @out;\n    for ^$n -> $i {\n        my @row;\n        for ^$n -> $j {\n            @row.push: $i == $j ?? 1 !! 0;\n        }\n        @out.push: @row;\n    }\n    @out;\n}\n\n# Determinant of a 3x3 matrix by cofactor expansion along the top row.\nsub det3(@m) {\n    @m[0][0] * (@m[1][1] * @m[2][2] - @m[1][2] * @m[2][1])\n  - @m[0][1] * (@m[1][0] * @m[2][2] - @m[1][2] * @m[2][0])\n  + @m[0][2] * (@m[1][0] * @m[2][1] - @m[1][1] * @m[2][0]);\n}\n\nmy @a = [1, 2, 3], [4, 5, 6];\nmy @b = [7, 8], [9, 10], [11, 12];\n\nshow(@a, 'A (2x3):');\nshow(@b, 'B (3x2):');\nshow(multiply(@a, @b), 'A * B (2x2):');\nshow(transpose(@a), 'transpose of A (3x2):');\n\n# Multiplying by the identity leaves a matrix unchanged.\nmy @sq = [2, 0, 1], [3, 5, 4], [1, 1, 0];\nmy @i3 = identity(3);\nshow(@i3, 'I (3x3):');\nsay '';\nsay 'A square matrix, times I, is itself: ', multiply(@sq, @i3) eqv @sq;\n\nsay '';\nsay 'det of [[2,0,1],[3,5,4],[1,1,0]] = ', det3(@sq);\n"},
{"name": "quicksort", "category": "Algorithms", "desc": "Quicksort the functional way: pick a pivot, partition the rest into the", "code": "#!/usr/bin/env raku\n# Quicksort the functional way: pick a pivot, partition the rest into the\n# elements below and above it with `.grep`, recurse on each half, and glue\n# the sorted pieces back together. Shows recursion, `.grep` with a pointy\n# block, list flattening with `|`, and multi-dispatch as a base case.\n\n# Multi-dispatch gives us the empty-list base case for free: the narrower\n# candidate wins when the argument matches, so no explicit `if` is needed.\nmulti quicksort()        { () }\nmulti quicksort($pivot, *@rest) {\n    my @less = @rest.grep(* < $pivot);\n    my @more = @rest.grep(* >= $pivot);\n    |quicksort(|@less), $pivot, |quicksort(|@more);\n}\n\n# For contrast, a merge sort: split in half, sort each half, then merge the\n# two sorted runs by repeatedly taking the smaller head element.\nsub merge-sort(@xs) {\n    return @xs if @xs <= 1;\n    my $mid = @xs div 2;\n    merge(merge-sort(@xs[^$mid]), merge-sort(@xs[$mid .. *]));\n}\nsub merge(@a, @b) {\n    my @out;\n    my ($i, $j) = 0, 0;\n    while $i < @a && $j < @b {\n        if @a[$i] <= @b[$j] {\n            @out.push: @a[$i++];\n        }\n        else {\n            @out.push: @b[$j++];\n        }\n    }\n    |@out, |@a[$i .. *], |@b[$j .. *];\n}\n\nmy @data = 5, 3, 8, 1, 9, 2, 7, 4, 6, 0;\nsay 'Input:      ', @data;\n\nmy @quick = quicksort(|@data);\nmy @mergey = merge-sort(@data);\nmy @builtin = @data.sort;\n\nsay 'quicksort:  ', @quick;\nsay 'merge-sort: ', @mergey;\nsay 'builtin:    ', @builtin;\n\n# Confirm every method agrees with the built-in `.sort`.\nsay '';\nsay 'quicksort matches .sort?  ', @quick eqv @builtin;\nsay 'merge-sort matches .sort? ', @mergey eqv @builtin;\n\n# The whole algorithm also fits on one line as a recursive closure.\nmy &qs = -> @x {\n    @x <= 1 ?? @x !! (|qs(@x.grep(* < @x[0])), @x[0], |qs(@x[1..*].grep(* >= @x[0])))\n};\nsay '';\nsay 'one-liner:  ', qs(@data);\n"},
{"name": "hanoi", "category": "Algorithms", "desc": "Towers of Hanoi: move a stack of N disks from one peg to another, never", "code": "#!/usr/bin/env raku\n# Towers of Hanoi: move a stack of N disks from one peg to another, never\n# placing a larger disk on a smaller one. The recursive insight is tiny --\n# to move N disks, move the top N-1 out of the way, move the biggest, then\n# move the N-1 back on top. Shows recursion and a running move counter.\n\nmy $moves = 0;\n\nsub hanoi($n, $from, $to, $via) {\n    return if $n == 0;\n    # Move the top n-1 disks off onto the spare peg.\n    hanoi($n - 1, $from, $via, $to);\n    # Move the largest remaining disk to its destination.\n    $moves++;\n    say \"  move {$moves.fmt('%2d')}: disk $n   $from -> $to\";\n    # Bring the n-1 disks back on top.\n    hanoi($n - 1, $via, $to, $from);\n}\n\nmy $disks = 4;\nsay \"Towers of Hanoi with $disks disks:\";\nsay '';\nhanoi($disks, 'A', 'C', 'B');\n\nsay '';\nsay \"Solved in $moves moves.\";\n# A stack of N disks always takes exactly 2^N - 1 moves -- the minimum.\nsay \"Minimum for $disks disks is 2^$disks - 1 = {2 ** $disks - 1}.\";\nsay 'Optimal? ', $moves == 2 ** $disks - 1;\n"},
{"name": "nqueens", "category": "Algorithms", "desc": "The N-Queens puzzle: place N queens on an N by N board so that no two", "code": "#!/usr/bin/env raku\n# The N-Queens puzzle: place N queens on an N by N board so that no two\n# share a row, column, or diagonal. This walks the classic backtracking\n# search — one queen per row, extending a partial placement column by\n# column and pruning any position that clashes with a queen already down.\n#\n# Shows off recursion, array building, and the junction-free `none`/`grep`\n# style of checking a candidate against every placement made so far.\n\nconstant \\N = 8;\n\n# @placed[$row] = the column chosen for the queen in that row.\n# A new queen at ($row, $col) is safe when no earlier queen sits in the\n# same column or on either diagonal (row-col and row+col are constant\n# along the two diagonal directions).\nsub safe(@placed, $col) {\n    my $row = @placed.elems;\n    for ^$row -> $r {\n        my $c = @placed[$r];\n        return False if $c == $col;\n        return False if $r - $c == $row - $col;\n        return False if $r + $c == $row + $col;\n    }\n    True;\n}\n\nmy $count = 0;\nmy @first;\n\nsub solve(@placed) {\n    if @placed.elems == N {\n        $count++;\n        @first = @placed.List if @first.elems == 0;\n        return;\n    }\n    for ^N -> $col {\n        if safe(@placed, $col) {\n            my @next = @placed.clone;\n            @next.push($col);\n            solve(@next);\n        }\n    }\n}\n\nsolve([]);\n\nsay \"Solutions for N=\" ~ N ~ \": \" ~ $count;\nsay '';\nsay 'One solution:';\nfor @first -> $col {\n    my @row = '.' xx N;\n    @row[$col] = 'Q';\n    say @row.join(' ');\n}\n"},
{"name": "brainfuck", "category": "Algorithms", "desc": "A tiny Brainfuck interpreter. Brainfuck has just eight instructions that", "code": "#!/usr/bin/env raku\n# A tiny Brainfuck interpreter. Brainfuck has just eight instructions that\n# operate on a tape of byte cells and a single data pointer:\n#\n#   >  move the pointer right      <  move the pointer left\n#   +  increment the cell          -  decrement the cell\n#   .  output the cell as a char   ,  read a char (unused here)\n#   [  jump past the matching ]    ]  jump back to the matching [\n#      if the current cell is 0       if the current cell is non-zero\n#\n# The embedded program below is the classic \"Hello World!\" — the loops set\n# up the ASCII codes on the tape, and the `.` commands print them.\n#\n# Shows off array/pointer manipulation, character/string building, and a\n# small dispatch loop with matched-bracket control flow.\n\nmy $program =\n    '++++++++[>++++[>++>+++>+++>+<<<<-]>+>+>->>+[<]<-]' ~\n    '>>.>---.+++++++..+++.>>.<-.<.+++.------.--------.>>+.>++.';\n\nsub run($code) {\n    my @prog = $code.comb;\n    my @tape = 0 xx 30000;\n    my $ptr  = 0;\n    my $pc   = 0;\n    my $out  = '';\n\n    while $pc < @prog.elems {\n        my $cmd = @prog[$pc];\n        if $cmd eq '>' {\n            $ptr++;\n        }\n        elsif $cmd eq '<' {\n            $ptr--;\n        }\n        elsif $cmd eq '+' {\n            @tape[$ptr] = (@tape[$ptr] + 1) % 256;\n        }\n        elsif $cmd eq '-' {\n            @tape[$ptr] = (@tape[$ptr] + 255) % 256;\n        }\n        elsif $cmd eq '.' {\n            $out ~= chr(@tape[$ptr]);\n        }\n        elsif $cmd eq '[' {\n            if @tape[$ptr] == 0 {\n                # Skip forward to the matching ']'.\n                my $depth = 1;\n                while $depth > 0 {\n                    $pc++;\n                    $depth++ if @prog[$pc] eq '[';\n                    $depth-- if @prog[$pc] eq ']';\n                }\n            }\n        }\n        elsif $cmd eq ']' {\n            if @tape[$ptr] != 0 {\n                # Jump back to the matching '['.\n                my $depth = 1;\n                while $depth > 0 {\n                    $pc--;\n                    $depth++ if @prog[$pc] eq ']';\n                    $depth-- if @prog[$pc] eq '[';\n                }\n            }\n        }\n        $pc++;\n    }\n    $out;\n}\n\nprint run($program);\n"},
{"name": "anagrams", "category": "Text & strings", "desc": "Anagram classes. Two words are anagrams when they are the same multiset of", "code": "#!/usr/bin/env raku\n# Anagram classes. Two words are anagrams when they are the same multiset of\n# letters, so the trick is to give every word a canonical key — its letters\n# sorted into order — and let words with the same key fall into the same\n# bucket. `.classify` does exactly that grouping; the rest is string ops\n# (`.comb.sort.join`) and a deterministic sort of the buckets for printing.\n\nmy @words = <\n    listen silent enlist tinsel   evil live vile veil\n    stop tops pots spot opts post  night thing\n    cat act tac                    angle glean angel\n    dog god                        rat tar art\n    cab bac                        stressed desserts\n>;\n\n# The canonical key: sort a word's letters. \"listen\" and \"silent\" both map\n# to \"eilnst\", so `.classify` drops them into the same group.\nmy %groups = @words.classify(*.comb.sort.join);\n\nsay \"Anagram groups (largest first):\";\nsay '';\n\n# Keep only the real anagram groups (two or more words), and print them\n# biggest-first, breaking ties by key so the output is always the same.\nfor %groups.pairs.sort({ $^b.value.elems <=> $^a.value.elems\n                         || $^a.key leg $^b.key }) -> $group {\n    next if $group.value.elems < 2;\n    printf \"  %-9s  %s\\n\", $group.key, $group.value.sort.join(' ');\n}\n\nsay '';\nmy $classes = %groups.pairs.grep(*.value.elems >= 2).elems;\nsay \"Found $classes anagram classes among {@words.elems} words.\";\n"},
{"name": "wordcount", "category": "Text & strings", "desc": "Word-frequency analysis, the \"wc | sort | uniq -c\" of a single expression.", "code": "#!/usr/bin/env raku\n# Word-frequency analysis, the \"wc | sort | uniq -c\" of a single expression.\n# A paragraph is lower-cased and chopped into words with a regex (so commas\n# and full stops fall away), tallied into a `Bag` — Raku's built-in multiset,\n# which counts occurrences for you — and the most common words are drawn as a\n# little ASCII bar chart. Ties are broken alphabetically so the output is\n# stable from run to run.\n\nmy $text = q:to/END/;\n    The fog comes on little cat feet. It sits looking over harbour and city\n    on silent haunches and then moves on. The fog is grey, the fog is soft,\n    the fog is everywhere the city looks; and the little cat of the fog moves\n    on and on over the harbour, over the city, and then the fog is gone.\n    END\n\n# Split into words: lower-case, then `.comb` out maximal runs of letters.\n# Feeding that list to `.Bag` gives a multiset mapping word => count.\nmy $counts = $text.lc.comb(/ <[a..z']>+ /).Bag;\n\nmy $total  = $counts.total;\nmy $unique = $counts.elems;\nsay \"Words: $total total, $unique unique.\";\nsay '';\n\n# Top ten, most frequent first (ties broken by the word itself). The bar is\n# just the count rendered as that many '#'.\nsay \"Most frequent words:\";\nfor $counts.pairs.sort({ $^b.value <=> $^a.value || $^a.key leg $^b.key })[^10] -> $w {\n    printf \"  %-10s %2d  %s\\n\", $w.key, $w.value, '#' x $w.value;\n}\n"},
{"name": "cipher", "category": "Text & strings", "desc": "Three classic substitution ciphers, and a proof that each one round-trips:", "code": "#!/usr/bin/env raku\n# Three classic substitution ciphers, and a proof that each one round-trips:\n# encrypting a message and then decrypting it returns the original text.\n#\n# ROT13 and the Caesar shift are one-liners with `.trans`, which maps ranges\n# of characters onto other ranges. The Vigenere cipher needs a running key,\n# so it drops down to per-character modular arithmetic with `.ord`/`.chr`.\n\nmy $message = 'Attack at Dawn!';\n\n# ROT13: rotate each letter halfway round the alphabet. It is its own inverse,\n# so the same transform both hides and reveals the text.\nsub rot13($s) { $s.trans('a..zA..Z' => 'n..za..mN..ZA..M') }\n\n# Caesar shift by N: build the shifted alphabets and map onto them. Decrypting\n# is just a shift by 26 - N (equivalently, -N modulo 26).\nsub caesar($s, $n) {\n    my $lo = ('a' .. 'z').join;\n    my $up = ('A' .. 'Z').join;\n    my $rl = $lo.comb.rotate($n).join;\n    my $ru = $up.comb.rotate($n).join;\n    $s.trans(\"$lo$up\" => \"$rl$ru\");\n}\n\n# Vigenere: each letter is shifted by the next letter of a repeating key. The\n# key index only advances on letters, so spaces and punctuation pass through.\nsub vigenere($text, $key, $dir) {\n    my @k = $key.uc.comb.map({ .ord - 65 });\n    my $ki = 0;\n    my $out = '';\n    for $text.comb -> $c {\n        if $c ~~ /<[A..Za..z]>/ {\n            my $base  = $c ~~ /<[A..Z]>/ ?? 65 !! 97;\n            my $shift = @k[$ki % @k.elems] * $dir;\n            $out ~= (($c.ord - $base + $shift) % 26 + $base).chr;\n            $ki++;\n        }\n        else {\n            $out ~= $c;\n        }\n    }\n    $out;\n}\n\nsay \"message: $message\";\nsay '';\n\nmy $r = rot13($message);\nsay 'ROT13';\nsay \"  encrypt: $r\";\nsay \"  decrypt: {rot13($r)}\";\nsay \"  round-trips: {rot13($r) eq $message}\";\nsay '';\n\nmy $c = caesar($message, 3);\nsay 'Caesar (shift 3)';\nsay \"  encrypt: $c\";\nsay \"  decrypt: {caesar($c, -3)}\";\nsay \"  round-trips: {caesar($c, -3) eq $message}\";\nsay '';\n\nmy $v = vigenere($message, 'LEMON', 1);\nsay 'Vigenere (key LEMON)';\nsay \"  encrypt: $v\";\nsay \"  decrypt: {vigenere($v, 'LEMON', -1)}\";\nsay \"  round-trips: {vigenere($v, 'LEMON', -1) eq $message}\";\n"},
{"name": "roman", "category": "Text & strings", "desc": "Roman numerals in both directions: integer to numeral and back again,", "code": "#!/usr/bin/env raku\n# Roman numerals in both directions: integer to numeral and back again,\n# round-tripping a set of numbers so that both conversions must agree.\n#\n# The forward direction is table-driven: a list of (value, symbol) pairs in\n# descending order, including the subtractive forms (CM, XL, IV, ...), is\n# greedily subtracted from the number. The reverse walks the letters and uses\n# a `given`/`when` to decide whether each one adds or subtracts.\n\n# Ordered largest-first so the greedy loop always takes the biggest chunk.\nmy @table =\n    1000 => 'M', 900 => 'CM', 500 => 'D', 400 => 'CD',\n     100 => 'C',  90 => 'XC',  50 => 'L',  40 => 'XL',\n      10 => 'X',   9 => 'IX',   5 => 'V',   4 => 'IV',\n       1 => 'I';\n\nsub to-roman($n is copy) {\n    my $out = '';\n    for @table -> $pair {\n        while $n >= $pair.key {\n            $out ~= $pair.value;\n            $n   -= $pair.key;\n        }\n    }\n    $out;\n}\n\nsub value-of($letter) {\n    given $letter {\n        when 'M' { 1000 }\n        when 'D' {  500 }\n        when 'C' {  100 }\n        when 'L' {   50 }\n        when 'X' {   10 }\n        when 'V' {    5 }\n        when 'I' {    1 }\n    }\n}\n\n# A smaller letter placed before a larger one is subtracted (IV = 5 - 1),\n# otherwise it is added. One left-to-right pass is enough.\nsub from-roman($s) {\n    my @letters = $s.comb;\n    my $total   = 0;\n    for ^@letters.elems -> $i {\n        my $here = value-of(@letters[$i]);\n        if $i + 1 < @letters.elems && $here < value-of(@letters[$i + 1]) {\n            $total -= $here;\n        }\n        else {\n            $total += $here;\n        }\n    }\n    $total;\n}\n\nmy @numbers = 4, 9, 14, 40, 90, 444, 1984, 2026, 3888;\n\nsay '  n     roman              back   ok';\nsay '  ----  -----------------  -----  --';\nfor @numbers -> $n {\n    my $roman = to-roman($n);\n    my $back  = from-roman($roman);\n    say sprintf('  %4d  %-17s  %4d   %s', $n, $roman, $back, $n == $back);\n}\n"},
{"name": "quine", "category": "Curiosities", "desc": "A quine: this program prints its own source exactly and reads nothing", "code": "#!/usr/bin/env raku\n# A quine: this program prints its own source exactly and reads nothing\n# from disk. The whole source is stored once in the string $s, and printf\n# feeds $s back into itself, filling in the quote marks and the text.\nmy $s = '#!/usr/bin/env raku\n# A quine: this program prints its own source exactly and reads nothing\n# from disk. The whole source is stored once in the string $s, and printf\n# feeds $s back into itself, filling in the quote marks and the text.\nmy $s = %c%s%c;\nprintf $s, 39, $s, 39;\n';\nprintf $s, 39, $s, 39;\n"}
];
