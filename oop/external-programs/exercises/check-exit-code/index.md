---
title: Exercise ’Check the exit code‘
---

{% include menu.html %}

## Problem

Run a command that fails on purpose — for example `sh -c 'exit 4'` — and keep the `Proc` it returns. Print the command’s exit code, and then print `success` or `failure` depending on whether the command succeeded.

## Example

The program prints:

```
4
failure
```

## Solution

✅ [See the solution](solution)

{% include nav.html %}
