---
title: Exercise ’Print without a newline‘
---

{% include menu.html %}

## Problem

Using the standard-output handle `$*OUT`, print the letters `a` and `b` with `print` (which adds no newline), and then the letter `c` with `say`. The three letters should end up on a single line.

## Example

The program prints:

```
abc
```

## Solution

✅ [See the solution](solution)

{% include nav.html %}
