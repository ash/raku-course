---
title: Exercise ’File or directory?‘
---

{% include menu.html %}

## Problem

Create a file called `a.txt`. Then ask the *same* path two questions: is it a regular file (`.f`), and is it a directory (`.d`)? Print both answers, to see that the two tests give opposite results for one path.

## Example

The program prints:

```
True
False
```

## Solution

✅ [See the solution](solution)

{% include nav.html %}
