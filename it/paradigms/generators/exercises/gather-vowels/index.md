---
title: Raccogliere con una condizione
translations_gpt:
---

{% include menu.html %}

## Problema

Usando `gather` e `take`, raccogli le vocali dalla parola `'education'`. Dividi la parola in caratteri con `.comb` e prendi ogni carattere che sia una vocale (`a`, `e`, `i`, `o` oppure `u`). Stampa l'elenco.

## Esempio

Il programma stampa:

```
[e u a i o]
```

## Soluzione

✅ [Vedi la soluzione](solution)

{% include nav.html %}
